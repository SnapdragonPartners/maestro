// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: verify.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listAuditArtifactsForVerify = `-- name: ListAuditArtifactsForVerify :many
SELECT artifact_id, organization_id, user_id, artifact_type, artifact_category, scope_type, scope_organization_id, scope_product_id, scope_feature_id, scope_epic_id, scope_story_id, scope_id, product_id, feature_id, epic_id, story_id, author_instance_id, produced_by_tool_call_id, schema_version, summary, payload, payload_digest, created_at FROM audit_artifacts
WHERE organization_id = $1
ORDER BY artifact_id
`

// ListAuditArtifactsForVerify returns one organization's Audit artifacts.
// This family has no review_digest by design (ADR 0021: exhaust, born
// final), so only payload_digest is recomputed for it.
func (q *Queries) ListAuditArtifactsForVerify(ctx context.Context, organizationID pgtype.UUID) ([]AuditArtifact, error) {
	rows, err := q.db.Query(ctx, listAuditArtifactsForVerify, organizationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AuditArtifact{}
	for rows.Next() {
		var i AuditArtifact
		if err := rows.Scan(
			&i.ArtifactID,
			&i.OrganizationID,
			&i.UserID,
			&i.ArtifactType,
			&i.ArtifactCategory,
			&i.ScopeType,
			&i.ScopeOrganizationID,
			&i.ScopeProductID,
			&i.ScopeFeatureID,
			&i.ScopeEpicID,
			&i.ScopeStoryID,
			&i.ScopeID,
			&i.ProductID,
			&i.FeatureID,
			&i.EpicID,
			&i.StoryID,
			&i.AuthorInstanceID,
			&i.ProducedByToolCallID,
			&i.SchemaVersion,
			&i.Summary,
			&i.Payload,
			&i.PayloadDigest,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listBinaryAttachmentsForVerify = `-- name: ListBinaryAttachmentsForVerify :many
SELECT attachment_id, organization_id, object_digest, media_type, size_bytes, created_at FROM binary_attachments
WHERE organization_id = $1
ORDER BY attachment_id
`

// ListBinaryAttachmentsForVerify returns EVERY attachment row in one
// organization.
//
// Every row, not the subset reachable from artifacts: "referenced" is
// ambiguous, and a row nothing currently points at is still a row whose
// blob has to exist for the plane to be internally consistent. Narrowing
// this to reachable rows would silently shrink what verification covers.
func (q *Queries) ListBinaryAttachmentsForVerify(ctx context.Context, organizationID pgtype.UUID) ([]BinaryAttachment, error) {
	rows, err := q.db.Query(ctx, listBinaryAttachmentsForVerify, organizationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []BinaryAttachment{}
	for rows.Next() {
		var i BinaryAttachment
		if err := rows.Scan(
			&i.AttachmentID,
			&i.OrganizationID,
			&i.ObjectDigest,
			&i.MediaType,
			&i.SizeBytes,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listManagementArtifactsForVerify = `-- name: ListManagementArtifactsForVerify :many
SELECT artifact_id, organization_id, user_id, artifact_type, artifact_category, status, scope_type, scope_organization_id, scope_product_id, scope_feature_id, scope_epic_id, scope_story_id, scope_id, product_id, feature_id, epic_id, story_id, author_instance_id, reviewer_instance_id, produced_by_tool_call_id, amends_artifact_id, supersedes_artifact_id, replaces_artifact_id, amendment_sequence, accepted_at, schema_version, summary, payload, payload_digest, review_digest, created_at, is_amendment, amends_target_is_amendment FROM management_artifacts
WHERE organization_id = $1
ORDER BY artifact_id
`

// ListManagementArtifactsForVerify returns one organization's Management
// artifacts, with every column the review projection is rebuilt from.
//
// SELECT * rather than a projection on purpose: review_digest is computed
// over a structure assembled from many columns, and a hand-listed set here
// would silently stop covering a column added later — the digest would then
// verify against a projection missing a field it was originally built with.
func (q *Queries) ListManagementArtifactsForVerify(ctx context.Context, organizationID pgtype.UUID) ([]ManagementArtifact, error) {
	rows, err := q.db.Query(ctx, listManagementArtifactsForVerify, organizationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ManagementArtifact{}
	for rows.Next() {
		var i ManagementArtifact
		if err := rows.Scan(
			&i.ArtifactID,
			&i.OrganizationID,
			&i.UserID,
			&i.ArtifactType,
			&i.ArtifactCategory,
			&i.Status,
			&i.ScopeType,
			&i.ScopeOrganizationID,
			&i.ScopeProductID,
			&i.ScopeFeatureID,
			&i.ScopeEpicID,
			&i.ScopeStoryID,
			&i.ScopeID,
			&i.ProductID,
			&i.FeatureID,
			&i.EpicID,
			&i.StoryID,
			&i.AuthorInstanceID,
			&i.ReviewerInstanceID,
			&i.ProducedByToolCallID,
			&i.AmendsArtifactID,
			&i.SupersedesArtifactID,
			&i.ReplacesArtifactID,
			&i.AmendmentSequence,
			&i.AcceptedAt,
			&i.SchemaVersion,
			&i.Summary,
			&i.Payload,
			&i.PayloadDigest,
			&i.ReviewDigest,
			&i.CreatedAt,
			&i.IsAmendment,
			&i.AmendsTargetIsAmendment,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listOrganizations = `-- name: ListOrganizations :many

SELECT organization_id, slug, display_name, created_at FROM organizations
ORDER BY organization_id
`

// Integrity verification (Phase 2 item 8).
//
// These statements serve `dataplane-verify`, which recomputes every stored
// digest after a restore. A restore's characteristic failure is a TORN PAIR
// — a Postgres cluster and an object store copied at moments that disagree —
// and neither store can detect that alone, so the check has to walk both.
//
// ON TENANCY. The rest of this directory is organization-scoped by rule: a
// statement without organization_id is one a caller in the wrong tenant can
// serve. Verification is plane-wide by nature, and the temptation is an
// unscoped `SELECT * FROM management_artifacts`. That temptation is refused
// here. ListOrganizations is the ONLY unscoped statement — an organization
// list is the scope vocabulary itself, not data inside a scope — and every
// statement below it is organization-scoped exactly like its neighbours.
// Verification therefore iterates organizations rather than reaching across
// them, and the tenancy rule survives an operator-level tool intact.
//
// ON BOUNDS. These are unbounded reads, which the accepted design permits at
// Phase 2 scale and flags in the exit record as needing keyset bounds before
// the plane holds real volume. They are deliberately NOT modelled on the
// call family's paged reads: a verification that skipped a page would report
// a healthy plane it had not finished checking, which is worse than a slow
// one.
// ListOrganizations enumerates the scopes verification walks.
//
// Ordered so two runs over an unchanged plane report in the same order,
// which is what makes a diff between two verification runs meaningful.
func (q *Queries) ListOrganizations(ctx context.Context) ([]Organization, error) {
	rows, err := q.db.Query(ctx, listOrganizations)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Organization{}
	for rows.Next() {
		var i Organization
		if err := rows.Scan(
			&i.OrganizationID,
			&i.Slug,
			&i.DisplayName,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
