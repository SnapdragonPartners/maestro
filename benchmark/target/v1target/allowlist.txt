# Reviewed NON-prompt files matched by the prompt scanner's heuristics
# (false positives). Entries here are NOT hashed into P — putting them in
# manifest.txt instead would make prompt identity move when non-prompt
# code changes. Every scanner candidate must appear in exactly one of the
# two lists; an unclassified candidate fails prompthash_test.go.
